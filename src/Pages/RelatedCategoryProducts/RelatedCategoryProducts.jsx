import { Helmet } from "react-helmet"; import Card from "../../Components/Card/Card"; import Loading from "../../Components/Loading/Loading"; import useRelatedProducts from "../../hooks/useRelatedProducts"; export default function RelatedCategoryProducts() { let { data, isLoading } = useRelatedProducts(); if (isLoading) return <Loading />; return ( <> <Helmet> <title>Fresh Cart | Related Category Products</title> <meta name="description" content="Related Category Products page of E-commerce website " /> <meta name="keywords" content="E-commerce, Related Category Products, Related Category Products page" /> </Helmet> {data?.data?.data?.length === 0 ? ( <div className="text-center text-2xl text-sky-600 py-13"> <h1 className="font-semibold"> <span>🚫 </span> No Related Products <span>🚫 </span> </h1> </div> ) : ( <div className="grid grid-cols-1 mt-5 md:grid-cols-4 gap-5 lg:grid-cols-6"> {data?.data?.data?.map((product) => ( <Card key={product.id} productInfo={product} /> ))} </div> )} </> ); } 